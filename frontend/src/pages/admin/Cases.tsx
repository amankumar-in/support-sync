import { Container, Typography } from "@mui/material";

const Cases = () => {
  return (
    <Container>
      <Typography variant="h4">Cases</Typography>
      <Typography>List and manage cases here.</Typography>
    </Container>
  );
};

export default Cases;
export {}; // ✅ Fix for --isolatedModules
