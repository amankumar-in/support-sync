import { Container, Typography } from "@mui/material";

const Reports = () => {
  return (
    <Container>
      <Typography variant="h4">Reports</Typography>
      <Typography>Generate and view system reports.</Typography>
    </Container>
  );
};

export default Reports;
export {}; // ✅ Fix for --isolatedModules
