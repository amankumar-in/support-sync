import { Container, Typography } from "@mui/material";

const Organization = () => {
  return (
    <Container>
      <Typography variant="h4">Organization Settings</Typography>
      <Typography>
        Manage your organization’s details and preferences.
      </Typography>
    </Container>
  );
};

export default Organization; // ✅ Ensure this is present
